package com.bms.www.repo;

@Repository
public interface AccountRepository extends JpaRepository<Account, Long> {
    // This helps us find the account when the user logs in
    Account findByUserUsername(String username);
}